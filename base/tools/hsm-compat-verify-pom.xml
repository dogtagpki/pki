<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>org.dogtagpki.pki</groupId>
    <artifactId>pki-hsm-compat-verify</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>PKI HSM Compatibility Verification</name>
    <description>HSM/PKCS#11 compatibility verification tool for Dogtag PKI KRA</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <jss.jar.path>/usr/lib/java/jss.jar</jss.jar.path>
    </properties>

    <dependencies>
        <!-- PKI CertSrv (replaces pki-common in 10.13) -->
        <dependency>
            <groupId>org.dogtagpki.pki</groupId>
            <artifactId>pki-certsrv</artifactId>
            <version>10.13.11</version>
            <scope>system</scope>
            <systemPath>/usr/share/java/pki/pki-certsrv.jar</systemPath>
        </dependency>

        <!-- PKI CMSUtil (replaces pki-common in 10.13) -->
        <dependency>
            <groupId>org.dogtagpki.pki</groupId>
            <artifactId>pki-cmsutil</artifactId>
            <version>10.13.11</version>
            <scope>system</scope>
            <systemPath>/usr/share/java/pki/pki-cmsutil.jar</systemPath>
        </dependency>

        <!-- JSS -->
        <dependency>
            <groupId>org.dogtagpki.jss</groupId>
            <artifactId>jss-base</artifactId>
            <version>4.9.9</version>
            <scope>system</scope>
            <systemPath>${jss.jar.path}</systemPath>
        </dependency>

        <!-- Apache Commons CLI -->
        <dependency>
            <groupId>commons-cli</groupId>
            <artifactId>commons-cli</artifactId>
            <version>1.4</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <sourceDirectory>src/main/java</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.12.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                    <includes>
                        <include>com/netscape/cmstools/hsmCompatVerifyServ.java</include>
                        <include>com/netscape/cmstools/hsmCompatVerifyClnt.java</include>
                        <include>com/netscape/cmstools/CryptoToolsUtil.java</include>
                    </includes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Implementation-Version>${project.version}</Implementation-Version>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
        <finalName>pki-hsm-compat-verify</finalName>
    </build>

</project>
