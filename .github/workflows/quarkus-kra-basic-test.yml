name: Basic Quarkus KRA

on: workflow_call

env:
  DS_IMAGE: ${{ vars.DS_IMAGE || 'quay.io/389ds/dirsrv' }}

jobs:
  # Smoke test for KRA subsystem running on Quarkus
  test:
    name: Test
    runs-on: ubuntu-latest
    env:
      SHARED: /tmp/workdir/pki
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Retrieve PKI images
        uses: actions/cache@v4
        with:
          key: pki-images-${{ github.sha }}
          path: pki-images.tar

      - name: Load PKI images
        run: docker load --input pki-images.tar

      - name: Create network
        run: docker network create example

      - name: Set up DS container
        run: |
          tests/bin/ds-create.sh \
              --image=${{ env.DS_IMAGE }} \
              --hostname=ds.example.com \
              --network=example \
              --network-alias=ds.example.com \
              --password=Secret.123 \
              ds

      - name: Set up CA container (Tomcat, for issuing certs)
        run: |
          tests/bin/runner-init.sh \
              --hostname=ca.example.com \
              --network=example \
              --network-alias=ca.example.com \
              ca

      - name: Install CA
        run: |
          docker exec ca pkispawn \
              -f /usr/share/pki/server/examples/installation/ca.cfg \
              -s CA \
              -D pki_ds_url=ldap://ds.example.com:3389 \
              --debug

      - name: Set up KRA Quarkus container
        run: |
          docker run \
              --name kra-quarkus \
              --hostname=kra.example.com \
              --network=example \
              --network-alias=kra.example.com \
              -d \
              -e QUARKUS_HTTP_PORT=8080 \
              -e QUARKUS_HTTP_SSL_PORT=8443 \
              -e QUARKUS_HTTP_INSECURE_REQUESTS=enabled \
              pki-quarkus-kra

      - name: Wait for KRA Quarkus to start
        run: |
          echo "Waiting for KRA Quarkus to start..."
          for i in $(seq 1 30); do
            if docker exec kra-quarkus curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/q/health 2>/dev/null | grep -q "200"; then
              echo "KRA Quarkus is ready"
              break
            fi
            if [ "$i" -eq 30 ]; then
              echo "KRA Quarkus failed to start"
              docker logs kra-quarkus
              exit 1
            fi
            echo "  Attempt $i/30..."
            sleep 5
          done

      - name: Verify KRA Quarkus endpoint
        run: |
          echo "Checking KRA Quarkus /v2/info endpoint..."
          docker exec kra-quarkus curl -s -o /dev/null -w "HTTP status: %{http_code}\n" \
              http://localhost:8080/v2/info || echo "info endpoint not yet available (may require configuration)"

      - name: Verify KRA Quarkus is running
        run: |
          echo "Checking KRA Quarkus process..."
          docker exec kra-quarkus ps aux | grep quarkus || true

          echo "Checking KRA Quarkus HTTP port..."
          docker exec kra-quarkus curl -s -o /dev/null -w "HTTP status: %{http_code}\n" \
              http://localhost:8080/q/health || echo "health endpoint not available"

      - name: Check KRA Quarkus container logs
        if: always()
        run: |
          docker logs kra-quarkus

      - name: Check DS server systemd journal
        if: always()
        run: |
          docker exec ds journalctl -x --no-pager -u dirsrv@localhost.service

      - name: Check DS container logs
        if: always()
        run: |
          docker logs ds

      - name: Check CA server systemd journal
        if: always()
        run: |
          docker exec ca journalctl -x --no-pager -u pki-tomcatd@pki-tomcat.service

      - name: Remove KRA Quarkus container
        if: always()
        run: |
          docker stop kra-quarkus || true
          docker rm kra-quarkus || true

      - name: Remove CA
        if: always()
        run: |
          docker exec ca pkidestroy -s CA --debug || true

      - name: Remove DS
        if: always()
        run: |
          docker stop ds || true
          docker rm ds || true

      - name: Remove network
        if: always()
        run: |
          docker network rm example || true
